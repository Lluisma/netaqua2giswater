
-- TAULA BUIDA

CREATE OR REPLACE VIEW EXT_PLOT AS

    SELECT NULL              id,
           NULL              plot_code,
           NULL              muni_id,
           NULL              postcode,
           NULL              streetaxis_id,
           NULL              postnumber,
           NULL              complement,
           NULL              placement,
           NULL              square,
           NULL              observ,
           NULL              text,
           NULL              the_geom,
           NULL              expl_id
    FROM   DUAL
    WHERE  1 = 2;
  